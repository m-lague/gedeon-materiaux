<!DOCTYPE html>
<html>
  <head>
    <title>GedeonMateriaux</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">


    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag 'rails-ujs', 'data-turbolinks-track': 'reload' %>
    <%= javascript_importmap_tags %>
    <script>
      window.addEventListener("load", function(){
      window.cookieconsent.initialise({
      "palette": {
        "popup": {
          "background": "#efefef",
          "text": "#404040"
        },
        "button": {
          "background": "#8ec760",
          "text": "#ffffff"
        }
      },
      "content": {
        "message": "This website uses cookies to ensure you get the best experience on our website.",
        "dismiss": "Got it!",
        "link": "Learn more",
        "href": "http://cookies.insites.com/about-cookies"
      }
      })});
    </script>
    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-5FHQB22XMV"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-5FHQB22XMV');
    </script>
  </head>

  <body>

      <%# <p class="notice"><%= notice %>
    <div class="main-container">
      <div class ="sidebar" id="sidebar">
        <%=link_to "https://gedeon.guide" do %>
          <%= image_tag("logo.png", id: "logo") %>
        <%end%>
        <ul>
          <li><%= link_to 'Materiaux', materials_path, class: "step btn btn-menu" %></li>
          <li><%= link_to 'Fournisseurs', suppliers_path, class: "step btn btn-menu" %></li>
          <% if user_signed_in? %>
            <li><%= link_to 'Déconnexion', destroy_user_session_path, method: :delete, class:"step btn btn-menu" %></li>
          <% else %>
            <li><%= link_to 'Connexion', new_user_session_path, class: "step btn btn-menu" %></li>
          <% end %>
          <div class="highlight">
                <%# rectangle vert %>
          </div>
        </ul>
      </div>
      <div class="burger-menu" id="burger-menu">
        <!-- Menu burger -->
        <button onclick="toggleSidebar()">☰ Menu</button>
      </div>
      <div class="content-container">
        <p class="alert"><%= alert %></p>
        <%= yield %>
        <% if current_user.present? %>
          <div class="ask-feedback">POUR TOUTE REMONTÉE D'INFORMATION (BUGS, SUGGESTIONS D'AMÉLIORATION, ...)
            ENVOYEZ MOI UN EMAIL À
            <a href="mailto:mathieu@gedeon.guide">mathieu@gedeon.guide</a>
          </div>
        <% end %>
      </div>
    </div>
    </div>
  </body>
</html>
